name: Publish current test Hub 20260902-01

on:
  push:
    branches:
      - fix/relay-nofile-upgrade-0.2.33-v2

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Smoke-check Hub runtime
        run: |
          python -m pip install --disable-pip-version-check -r requirements.txt
          python -m py_compile hub.py hub_entry.py stun_service.py stun_port_config_patch.py tcp_session_service.py
          python -c 'import hub_entry; print("Hub smoke check OK", hub_entry.HUB_VERSION)'

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push test Hub image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/labprobe-hub:test-20260902-01
