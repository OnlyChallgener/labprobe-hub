# LabProbe Hub v0.8.3

- 端口映射规则新增 `leaseSeconds`，与绝对到期时间分开保存。
- 定时规则到期后再次启动，会按上次选择的有效期重新计时。
- 旧 v0.8.2 规则会尽量根据创建时间与到期时间推导原有效期。
- 规则状态对账新增 `leaseSeconds` 字段，不改变现有 APP/Agent/Rust 接口路径。
- Docker 镜像标签统一更新为 `0.8.3`。
