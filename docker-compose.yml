services:
  mqtt-init:
    extends:
      file: docker-compose.host.yml
      service: mqtt-init
  labprobe-mqtt:
    extends:
      file: docker-compose.host.yml
      service: labprobe-mqtt
  labprobe-hub:
    extends:
      file: docker-compose.host.yml
      service: labprobe-hub
